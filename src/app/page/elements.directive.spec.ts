import { PageElementsDirective } from './elements.directive';

describe('PageElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new PageElementsDirective();
    expect(directive).toBeTruthy();
  });
});
